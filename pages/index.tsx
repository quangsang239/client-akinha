import type { NextPage } from "next";

import { MainLayout } from "../components/layout";

const Home: NextPage = () => {
  return (
    <div>
      <MainLayout />
    </div>
  );
};

export default Home;
